# Node.js dependencies
/node_modules

# Other package managers (this repo uses pnpm only — see SECURITY.md)
package-lock.json
yarn.lock
npm-shrinkwrap.json
bun.lock
bun.lockb

# Build output
/dist
/build
/lib

# Logs and temporary files
*.log
.DS_Store
.env
.env.local
.env.*.local

# IDE and editor files
.vscode
.idea
*.sublime-project
*.sublime-workspace

# n8n specific files (if applicable)
# For example, if you have local test data or configuration that should not be committed
# /test-data
# /config.json
