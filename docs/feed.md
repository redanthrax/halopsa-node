# Feed Operations

Activity feed entries (`/api/Feed`). OpenAPI supports list only.

## Get Many

**Return All**, **Limit**, and filters: **Client ID** (`related_client_id`), **Site ID**, **User ID**, **Ticket ID** (`one_agents_tickets_id`), **Asset ID**, **Agent Only**, **Followed Only**, **Exclude Private**, **Count**.
