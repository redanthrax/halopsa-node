# Contract Rule Operations

Contract rules (`/api/ContractRule`).

## Get Many / Get by ID / Create / Delete

Standard CRUD. **Rule ID** for get and delete. **Create** uses **Additional Fields** (object or array).
