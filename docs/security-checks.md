# Security Check Operations

Instance security diagnostics (`/api/SecurityCheck`).

## Get

Runs `GET /SecurityCheck` (no parameters).

## Get Old Encryption

Runs `GET /SecurityCheck/oldencryption` to report legacy encryption usage.
