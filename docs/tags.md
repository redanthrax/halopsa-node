# Tags

Ticket and entity tags (`/api/Tags`). Supports list, get by ID, create, and delete per OpenAPI.

## Operations

- **Get Many** — `GET /Tags`
- **Get by ID** — `GET /Tags/{id}`
- **Create** — `POST /Tags` with JSON body fields
- **Delete** — `DELETE /Tags/{id}`
