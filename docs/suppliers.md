# Supplier Operations

Suppliers (`/api/Supplier`, `Company` schema).

## Get Many

**Return All**, **Limit**, **Search**, **Include Active**, **Include Inactive**.

## Get by ID

**Supplier ID** with optional **Include Details**.

## Create

`POST /Supplier` with **Additional Fields** (object or array).

## Delete

Delete by **Supplier ID**.
