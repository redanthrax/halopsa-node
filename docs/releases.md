# Release Operations

Product releases (`/api/Release`).

## Get Many

**Return All**, **Limit**, **Search**, **Product ID**, **Include Details**.

## Get by ID

**Release ID** with optional **Include Details**.

## Create

`POST /Release` with **Additional Fields** (object or array).

## Delete

Delete by **Release ID**.
