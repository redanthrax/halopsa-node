# Item Operations

Product and service catalog items (`/api/Item`). List responses return a JSON array.

## Get Many

**Return All**, **Limit**, **Search**

## Get by ID / Create / Update / Delete

Standard CRUD with JSON **Additional Fields** / **Update Fields**.
