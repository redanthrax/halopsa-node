# Approval Process Operations

Workflow approval processes (`/api/ApprovalProcess`).

## Get Many

**Return All**, **Limit**, optional **Access Control Level**.

## Get by ID / Create / Delete

**Process ID** with optional **Include Details** on get. **Create** uses **Additional Fields** (object or array).
