# Survey Operations

List customer satisfaction survey responses (`GET /api/CustomerSatisfaction`). Equivalent to halopsa-mcp `halopsa_list_surveys`.

## Get Many

**Parameters:**
- **Return All** / **Limit** — Return All paginates through all pages; limited requests cap at 100 per API call
- **Ticket ID** — Optional filter (0 = all tickets)
