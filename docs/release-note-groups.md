# Release Note Group Operations

Release note grouping (`/api/ReleaseNoteGroup`).

## Get Many

**Return All** and **Limit**.

## Get by ID / Create / Delete

**Group ID** with optional **Include Details** on get. **Create** uses **Additional Fields** (object or array).
