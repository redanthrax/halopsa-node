# Approval Process Rule Operations

Rules for approval processes (`/api/ApprovalProcessRule`).

## Get Many

**Return All**, **Limit**, and filters: **Global**, **Process ID**, **Step ID**.

## Get by ID / Create / Delete

**Rule ID** with optional **Include Details** on get.
