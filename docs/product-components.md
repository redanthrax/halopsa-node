# Product Component Operations

Release components (`/api/ProductComponent`).

## Get Many

**Return All**, **Limit**, optional **Product ID**.

## Get by ID / Create / Delete

**Component ID** for get and delete. **Create** uses **Additional Fields** (`ReleaseComponent` object or array).
